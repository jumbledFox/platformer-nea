// A bunch of tiles, doors, etc...

pub mod tile;

pub struct Level {
    width: usize,
    height: usize,
    tiles: Vec<usize>,
    
    // signs (ill do this much later)
    // doors
    // player  start point
    // checkpoints
    // enemy   start points
    // powerup start points
}
